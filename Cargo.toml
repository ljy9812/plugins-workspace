[workspace]
members = [
  "plugins/*",
  "plugins/*/tests/*",
  "plugins/updater/tests/updater-migration/v2-app",
  "plugins/*/examples/*/src-tauri",
  "examples/*/src-tauri",
]
resolver = "2"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
tracing = "0.1"
log = "0.4"
tauri = { version = "2.10", default-features = false }
tauri-build = "2.5"
tauri-plugin = "2.5"
tauri-utils = "2.8"
serde_json = "1"
thiserror = "2"
url = "2"
schemars = "0.8"
dunce = "1"
specta = "^2.0.0-rc.16"
glob = "0.3"
zbus = "5.9"

[workspace.package]
edition = "2021"
authors = ["Tauri Programme within The Commons Conservancy"]
license = "Apache-2.0 OR MIT"
rust-version = "1.77.2"
repository = "https://github.com/tauri-apps/plugins-workspace"

# default to small, optimized release binaries
[profile.release]
panic = "abort"
codegen-units = 1
lto = true
incremental = false
opt-level = "s"

# Patch the Tauri stack to local forks with OHOS support: the crates.io
# releases of tauri/tauri-runtime/tauri-runtime-wry/wry/tao/muda/tray-icon lack
# `not(target_env = "ohos")` on their Linux gtk deps, which pulls gio-sys and
# breaks OHOS cross-compilation. Remove these patches once the OHOS changes
# are merged upstream and published to crates.io.
[patch.crates-io]
tauri = { path = "../tauri/crates/tauri" }
tauri-build = { path = "../tauri/crates/tauri-build" }
tauri-utils = { path = "../tauri/crates/tauri-utils" }
tauri-runtime = { path = "../tauri/crates/tauri-runtime" }
tauri-runtime-wry = { path = "../tauri/crates/tauri-runtime-wry" }
tauri-plugin = { path = "../tauri/crates/tauri-plugin" }
wry = { path = "../wry" }
tao = { path = "../tao" }
muda = { path = "../muda" }
tray-icon = { path = "../tray-icon" }
window-vibrancy = { path = "../window-vibrancy" }
openharmony-ability = { path = "../openharmony-ability/crates/ability" }
openharmony-ability-derive = { path = "../openharmony-ability/crates/derive" }
